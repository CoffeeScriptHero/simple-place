const SAVE_USER = "simple-place/user/SAVE_USER";

const SET_LOGGED = "simple-place/user/SET_LOGGED";

const UPDATE_MODAL = "simple-place/user/UPDATE_MODAL";

const typesObj = {
  SAVE_USER,
  SET_LOGGED,
  UPDATE_MODAL,
};

export default typesObj;

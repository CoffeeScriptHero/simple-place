const SAVE_USER = "simple-place/user/SAVE_USER";

const SET_NOT_FOUND = "simple-place/user/SET_NOT_FOUND";

const UPDATE_MODAL = "simple-place/user/UPDATE_MODAL";

const LOG_OUT = "simple-place/user/LOG_OUT";

const typesObj = {
  SAVE_USER,
  SET_NOT_FOUND,
  UPDATE_MODAL,
  LOG_OUT,
};

export default typesObj;

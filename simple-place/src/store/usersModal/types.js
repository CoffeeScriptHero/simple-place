const SET_SHOW_MODAL = "simple-place/usersModal/SET_SHOW_MODAL";

const SET_MODAL_TYPE = "simple-place/usersModal/SET_MODAL_TYPE";

const GET_FOLLOWERS = "simple-place/usersModal/GET_FOLLOWERS";

const GET_FOLLOWING = "simple-place/usersModal/GET_FOLLOWING";

const typesObj = {
  SET_SHOW_MODAL,
  SET_MODAL_TYPE,
  GET_FOLLOWERS,
  GET_FOLLOWING,
};

export default typesObj;
